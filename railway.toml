[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @fashion/api exec prisma generate && pnpm --filter @fashion/shared build && pnpm --filter @fashion/api build"

[deploy]
startCommand = "pnpm --filter @fashion/api start"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
